
---
> **「テスト失敗時はマージをブロックできる構成とする（ブランチ保護との連携を想定）」**

---

### 意味の解説

**正確には「プルリクを通さない」のではなく「マージできないようにブロックする」**です。

```
開発者が PR を作成          ← PR 自体は誰でも作れる
        ↓
GitHub Actions が自動テスト実行
        ↓
テスト失敗
        ↓
マージボタンがブロックされる  ← ここでブロック
（Merge できない状態になる）
```

---

### 仕組み（ブランチ保護との連携）

GitHub の **Branch Protection Rules（ブランチ保護ルール）** と組み合わせることで実現します。

| 設定項目 | 内容 |
|---------|------|
| Require status checks to pass | 指定した CI が ✅ にならないとマージ不可 |
| Require branches to be up to date | テスト対象ブランチが最新でないとマージ不可 |

---

### 設定場所

GitHub リポジトリの  
**Settings → Branches → Branch protection rules → Add rule**

`main` ブランチに対して「Status checks」に GitHub Actions のジョブ名を指定することで、**テストが通らない限り main へのマージが物理的にできない**状態にできます。

---
> ** AWS / GCP で「デプロイできる」になるまでのロードマップ **
---

### 前提として身につけること

まず AWS / GCP の前に以下が基礎として必要です。

| 分野 | 具体的な内容 |
|------|------------|
| Linux コマンド | ファイル操作・権限・プロセス管理 |
| ネットワーク基礎 | IP・DNS・HTTP/HTTPS・ポート・SSL |
| Docker | コンテナ起動・docker compose・イメージビルド |
| Git | ブランチ・PR・マージ（← 今ここ） |

---

### AWS / GCP で「デプロイできる」になるまでのロードマップ

#### STEP 1 — クラウドの概念を掴む
- IaaS / PaaS / SaaS の違い
- リージョン・アベイラビリティゾーンとは何か
- 仮想マシン（EC2 / Compute Engine）の概念

#### STEP 2 — AWS か GCP どちらかに絞って触る
**最初は AWS がおすすめ**（日本語情報・求人ともに多い）

```
AWS の最初に触るサービス
  EC2        → 仮想マシン（サーバー）
  RDS        → マネージド MySQL
  S3         → ファイルストレージ
  VPC        → ネットワーク設定
  IAM        → 権限管理（最重要）
  Route 53   → DNS 設定
```

#### STEP 3 — 手動デプロイを経験する
- EC2 に SSH でログイン
- PHP / Laravel を手動でインストール
- Nginx / Apache の設定
- MySQL に接続して動かす

**まず「手動でできる」ことが自動化への近道**

#### STEP 4 — GitHub Actions と連携して自動デプロイ
```yaml
# main マージ時に EC2 へ自動デプロイする例
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to EC2
        run: ssh ec2-user@xxx.xxx.xxx.xxx "cd /var/www && git pull"
```

---

### 学習コスト感の目安

| ステップ | 期間の目安 |
|---------|----------|
| Linux・ネットワーク基礎 | 2〜4週間 |
| Docker を使いこなす | 2〜4週間 |
| AWS 基礎（EC2・RDS・VPC） | 1〜2ヶ月 |
| 手動デプロイ成功 | 1〜2ヶ月 |
| CI/CD で自動デプロイ | 2〜4週間 |

---

### おすすめの学習順序

```
① Docker をしっかり理解する（今の環境で練習できる）
        ↓
② AWS 無料枠アカウントを作成して EC2 を触る
        ↓
③ Laravel アプリを EC2 に手動デプロイしてみる
        ↓
④ GitHub Actions でデプロイを自動化する（今のリポジトリが活きる）
        ↓
⑤ GCP / その他は必要になったら
```

---

### 注意点

- AWS は**触っているだけで課金されるサービスがある**ので無料枠の範囲を確認する
- IAM（権限管理）を最初にしっかり学ばないとセキュリティ事故につながる
- **GCP は AWS をある程度理解してから**でも遅くない（概念は共通）

今取り組んでいる GitHub Actions の CI 構築が完成すると、STEP 4 の自動デプロイへの理解がかなりスムーズになります。
